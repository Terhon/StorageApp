@model string

@{
    if (!string.IsNullOrWhiteSpace(Model))
    {
        var formatted = Html.Encode(Model).Replace(Environment.NewLine, "<br />").Replace("\n", "<br />");
        @Html.Raw(formatted)
    }
}